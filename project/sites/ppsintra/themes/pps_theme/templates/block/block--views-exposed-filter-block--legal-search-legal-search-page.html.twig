{#
/**
* @file
* Block template for exposed filter forms attached to search pages.
#}
{%
  set classes = [
  'block',
  'block-' ~ configuration.provider|clean_class,
  'block-' ~ plugin_id|clean_class,
]
%}

{% if label %}
  <h2>{{ label }}</h2>
{% endif %}
<div{{ attributes.addClass(classes) }}>
  {% set search_desc = 'Search for legal guidance and advice including PPS policies, staff instructions and Policy Information Notes.' %}
  <p>{{ search_desc|t }}</p>
  {% block content %}
    {% if content_attributes is not empty %}
      <div{{ content_attributes }}>
        {{ content }}
      </div>
    {% else %}
      {{ content }}
    {% endif %}
  {% endblock %}
</div>
<h2 class="visually-hidden">Filter search results</h2>
