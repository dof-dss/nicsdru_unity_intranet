adding:
  backend_config:
    connector_config:
      host: ddev-unity.intranet-solr
      skip_schema_check: true
      solr_install_dir: /opt/solr
      checkpoints_collection: ''
      stats_cache: org.apache.solr.search.stats.LRUStatsCache
      distrib: true
      context: solr
removing:
  backend_config:
    connector_config:
      host: ppsintra_solr.internal
      skip_schema_check: false
      solr_install_dir: ''
